import { prefix } from "../../utils/string";

const projectsPrefix = prefix("projects/");

export const GET_PROJECTS = projectsPrefix("GET_PROJECTS");
export const SET_PROJECTS = projectsPrefix("SET_PROJECTS");
